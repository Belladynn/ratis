# Ratis Doc Inventory

> Auto-generated — do not edit. Regenerate: `python scripts/generate-arch-inventory.py`.
> CI checks freshness on every PR (`.github/workflows/doc-inventories.yml`).
> Convention : CLAUDE.md R41 — every `## <ID> — <titre> · <refs> · <STATUT>` heading in
> tracked ARCH_*.md / docs/known/KNOWN_PROBLEMS.md / docs/decisions/DECISIONS_ACTED.md is indexed below.

> Agent rule (R29) : NEVER full-read this file — always `Grep` by tag/ID/status,
> then open the source file at the indicated line.

**110 entries** (28 EN-COURS · 66 LIVRÉ · 14 PLANIFIÉ · 2 LEGACY)

Format : `ID | STATUT | file:line | tags espacés | TL;DR`

ID | STATUT | FICHIER:LIGNE | TAGS | TL;DR
---+--------+---------------+------+------
ARCH_CLIENT | EN-COURS | ratis_client/ARCH_CLIENT.md:26 | frontend mobile expo react-native ui ios android expo-router eas eas-update typescript react-query i18next sentry | Ratis mobile application (Expo SDK54 + React Native + expo-router + TypeScript): 4 tabs (dashboard, list, scan, product, profile), 30+ React Query hooks, OAuth Google+Apple, OTA via EAS Update. Shared TS/JS code, limited native modules.
ARCH_OCR_LLM_BRIDGE | EN-COURS | webservices/ratis_product_analyser/ARCH_OCR_LLM_BRIDGE.md:11 | ocr llm paddleocr fuzzy-match knowledge-loop training local-first denoise-only cluster-by-bbox anti-hallucination design ocr_knowledge | OCR ↔ LLM pipeline **design phase v2**: local-first EAN matching (PaddleOCR + fuzzy), LLM downgraded to pure-denoise + classify residue cluster-by-bbox. No DB candidates as LLM input. Feedback loop via `ocr_knowledge`.
ARCH_admin_endpoints | EN-COURS | docs/arch/ARCH_admin_endpoints.md:1 | admin endpoints cross-service admin-api-key x-admin-operator audit-log parsed_tickets knowledge-tables override htmx alpha-beta draft | Cross-service ARCH for `/admin/*` endpoints (PA, AU, RW) + mini FastAPI+HTMX UI: observe (audit-log, parsed_tickets), curate (knowledge tables), validate (user_suggested stores), correct (admin override), replay, stats. Gated by `ADMIN_API_KEY` + `X-Admin-Operator`.
ARCH_auth | EN-COURS | ratis_client/ARCH_auth.md:11 | auth oauth client expo jwt secure-store apple-auth google refresh-singleton authcontext bearer login | Mobile auth stack: `expo-secure-store` (tokens), `expo-auth-session` + `expo-apple-authentication`, `api-client.ts` (Bearer inject + 401 refresh singleton), `AuthContext`. V1 complete, Sentry wiring + OAuth client IDs pending.
ARCH_boutique | EN-COURS | webservices/ratis_rewards/ARCH_boutique.md:11 | boutique shop gift-cards sink rotation runa cab spend caps fiscal-cap weekly-cap saison-1 v1 | User-initiated shop that closes the earn→spend loop in V1: the user spends their CAB on Runa gift cards (5 brands Season 1, denominations 5/10/20/50€), subject to fiscal/daily/weekly caps. Phase 1 backend shipped, frontend in progress.
ARCH_cab_economy | EN-COURS | docs/arch/ARCH_cab_economy.md:10 | cab economy multipliers coverage-bonus rewards gift-cards fiscal-cap transparency sink subscription-multiplier streak cab_service award_cab | CAB Economy V1: who emits, at which multipliers (coverage_bonus, subscription, streak), which fiscal/daily/weekly caps, how CAB is spent (sink = boutique gift-cards V1, missions/battlepass). Not the battlepass XP (separate), not gift-cards (outside initial V1).
ARCH_cashback | EN-COURS | webservices/ratis_rewards/ARCH_cashback.md:11 | cashback rewards payout webhook reconciliation affilae awin cj affiliate cashback_transactions cashback_withdrawals legal never-purge | Cashback affiliation (Affilae/Awin/CJ): click detection, conversion webhook, payout via withdrawals. Reconciliation to come via a dedicated batch. `cashback_transactions` + `cashback_withdrawals` never purgeable (legal).
ARCH_deployment | EN-COURS | docs/arch/ARCH_deployment.md:10 | deployment infra docker hetzner mac-mini caddy eas cloud-init docker-compose runbook self-hosted gh-actions aws migration-path backups | Multi-stage Ratis deployment strategy (Hetzner V0 → Mac mini → AWS), Docker Compose + Caddy topology, self-hosted GH Actions runner, cloud-init bootstrap, emergency runbook, secrets, backups, EAS Android APK.
ARCH_design_system | EN-COURS | ratis_client/ARCH_design_system.md:11 | design-system ui components theme client gamification duolingo clash-royale juicy 3d coin-cascades pattern-v2 | Mobile Design System: **Duolingo/Clash Royale** pivot confirmed 2026-05-03 (juicy, chunky 3D, coin cascades). Initial Figma teal pattern abandoned. Source of truth: `Ratis Design Pattern v2.html` committed.
ARCH_expo_strategy | EN-COURS | ratis_client/ARCH_expo_strategy.md:11 | expo strategy migration mobile eas eas-build expo-router managed-workflow eject signals | Mobile stack strategy: Expo managed + EAS Build + expo-router for V1. No custom native lib. Monitoring of red signals that would justify an ejection (native camera APIs, out-of-date modules, blocking perf issues).
ARCH_feed_jack | EN-COURS | ratis_client/ARCH_feed_jack.md:11 | feed-jack streak gamification client mascot jack idle hungry happy broken design-phase | `JackWidget` component (gamification mascot) on the home screen: idle/hungry/happy/broken states driven by the daily streak (DA-09 manual catch-up on reconnection). Frontend of the Feed Jack system documented on the `ARCH_gamification.md` side.
ARCH_gift_cards | EN-COURS | webservices/ratis_rewards/ARCH_gift_cards.md:11 | gift-cards rewards runa payout gift_card_brands gift_card_orders idempotent source-of-truth kyb v1 | Provisioning gift-cards via Runa (V1 post-KYB) : `gift_card_brands`, `gift_card_orders` with `UNIQUE(source_type,source_ref_id)` idempotent. Entry points from battlepass, shop, referral payout.
ARCH_itops | EN-COURS | docs/arch/ARCH_itops.md:46 | itops monitoring auto-update observability logs self-hosted mac-mini healthchecks watchtower uptime-kuma loki promtail docker | Self-hosted ops stack on Mac mini: Healthchecks (cron monitoring + email alerts), Watchtower (Docker image auto-update), Uptime Kuma (uptime status page), Loki+Promtail (centralised logs). Everything in `infra/itops/` Docker Compose.
ARCH_liste | EN-COURS | ratis_client/ARCH_liste.md:11 | liste shopping-list route-optim client itinerary build-optimize-shop scan-fab grouped-view theme-v2 | Shopping list screen: build → optimize → shop flow. Flat list wired to the backend, async optimisation + grouped multi-store view, scan-FAB to check off items. Theme v2 PR #52.
ARCH_llm_observability | EN-COURS | docs/arch/ARCH_llm_observability.md:23 | observability llm langfuse opentelemetry anthropic tracing eval ocr cost haiku self-hosted clickhouse rgpd | TL;DR: Ratis calls an LLM in production (Claude Haiku 4.5 in the OCR pipeline, `AnthropicLLMClient.extract`) with no observability. We instrument this call with **Langfuse self-hosted** (OTEL + `AnthropicInstrumentor`): tracing tokens/cost/latency/fallback, modelled on the no-op-if-empty pattern from `init_sentry`. Self-hosted is mandatory (receipt text is purchase data → GDPR). Roadmap: tracing → offline eval (ground-truth = `product_knowledge`) → A/B Haiku vs Sonnet.
ARCH_name_resolution_consensus | EN-COURS | webservices/ratis_product_analyser/ARCH_name_resolution_consensus.md:13 | consensus name-resolution matcher crowdsourcing knowledge product-analyser product_name_resolutions verified consensus-only refonte 2026-05 | Crowdsourced consensus `(store_id, normalized_label) → product_ean` that resolves receipt scans via `product_name_resolutions` (VERIFIED). Matcher redesigned to consensus-only on 2026-05-02: `products` becomes a lexical dictionary, no longer a fuzzy matcher. Blocks A-D done, E planned, F in-progress.
ARCH_product | EN-COURS | ratis_client/ARCH_product.md:11 | product ean prices favorites client product-analyser useProductByEan fiche-produit catalogue-statique | Product screen: EAN product sheet, local prices via consensus, favorites. Wired to the real API via `useProductByEan`. Static V1 catalogue fallback for incomplete product sheets.
ARCH_profil | EN-COURS | ratis_client/ARCH_profil.md:11 | profil account client menu stats avatar referral scan-history my-info logout delete-account theme-v2 | Profile screen: avatar/name/handle header, stats (cumulative savings, scans, CAB), menu (referral, scan-history, shop, my-info, logout, DELETE /account). Theme v2 PR #52.
ARCH_scan_history | EN-COURS | ratis_client/ARCH_scan_history.md:29 | scan-history receipt label client rescan infinite-query barcode-scanner-modal v0 v0.1 gps-per-image anti-fraud | Dedicated screen `/scan-history` accessible from `scan.tsx` ("See all →"). V0 display receipts/labels + barcode link + rescan. V0.1: per-image GPS fix for multi-store anti-fraud. `useInfiniteQuery` + `BarcodeScannerModal`.
RA-1 | EN-COURS | docs/arch/ARCH_RATIS.md:47 | summary identity vision cashback prices gamification gift-cards | TL;DR Ratis is a mobile app (Expo/React Native) backed by 5 FastAPI web services and 9 Python cron batches, delivering real cashback, OCR-sourced real-time product prices, a non-sellable gamification currency (cabecoin / CAB + battle pass), and — from V1 onward, post-Runa KYB — free gift cards funded by affiliate partnerships.
RA-2 | EN-COURS | docs/arch/ARCH_RATIS.md:62 | c4 system-context level1 external-systems boundary integrations | TL;DR Level-1 C4 view: one human actor (the shopper) drives the Expo mobile app, which talks to a single "Ratis Platform" boundary; the platform integrates eight external systems (Google/Apple OAuth, Stripe, Runa, OSM + OSRM, Cloudflare R2, Expo Push, Anthropic).
RA-3 | EN-COURS | docs/arch/ARCH_RATIS.md:100 | c4 container level2 fastapi celery postgres redis caddy terraform aws-boundary | TL;DR Level-2 C4 view: the Expo client calls five independently deployable FastAPI services through Caddy; PA and LO each run a separate Celery worker; all services share one PostgreSQL 16 and one Redis 7; PA additionally uses R2 and Anthropic, LO uses OSRM. The whole backend is provisioned by Terraform (`infra/aws`).
RA-4 | EN-COURS | docs/arch/ARCH_RATIS.md:169 | c4 deployment single-host mac-mini aws fargate rds elasticache terraform migration | TL;DR Two deployment topologies coexist: the **current** single-host setup (all containers on one Mac-mini M4 Pro via docker-compose + Caddy) and the **target** AWS topology (ECS Fargate tasks behind an ALB, RDS Postgres, ElastiCache Redis, Secrets Manager) codified in `infra/aws`. The migration path Hetzner → Mac-mini → AWS is documented with explicit saturation signals.
RA-5 | EN-COURS | docs/arch/ARCH_RATIS.md:219 | agentic mcp rag docs-mcp secrets-vault hermes n8n langfuse control-plane | TL;DR The headline differentiator: a typed, Keychain-backed MCP server is the single control plane through which the LLM operates Ratis without ever touching a raw secret. Around it sit a hybrid-RAG docs index (bge-m3 + sqlite-vec), a just-in-time secrets vault, the Hermes ops agent, an n8n-confined agent→production-DB write pipeline, and Langfuse tracing on the OCR→LLM call.
RA-6 | EN-COURS | docs/arch/ARCH_RATIS.md:256 | decisions adr trade-offs atam quality-attributes monorepo jwt psycopg int-cents | TL;DR The load-bearing decisions and the quality attribute each one trades. Full decision records (MADR) live in `docs/adr/`; this is the orientation table.
RA-7 | EN-COURS | docs/arch/ARCH_RATIS.md:275 | flows runtime scan login gift-card celery push fire-and-forget | TL;DR Three flows carry most of the product: receipt scan (OCR → consensus → reward), OAuth login (id_token → RS256 JWT), and gift-card claim (atomic balance → Runa). Each is asynchronous where the work is heavy.
RA-8 | EN-COURS | docs/arch/ARCH_RATIS.md:287 | quality-attributes gdpr rgpd availability security reliability privacy | TL;DR The non-functional spine: fire-and-forget availability, atomic materialized balances, single-issuer security, and an inflexible GDPR ruleset (no PII names, 48h receipt images, never-purge financial tables, in-place account anonymisation). The full six-pillar self-assessment lives in [[WELL_ARCHITECTED]].
RA-9 | EN-COURS | docs/arch/ARCH_RATIS.md:300 | catalogue services batches pointers directory | TL;DR The 5 services and 9 batches, each pointing to its owning ARCH. This section is a directory, not a duplication — depth lives in the linked docs.
ARCH_AUTH | LIVRÉ V0 | webservices/ratis_auth/ARCH_AUTH.md:27 | auth oauth google apple jwt redis refresh-token stripe subscription account-deletion login register rgpd | FastAPI service (port 8001) responsible for Ratis user identity: OAuth Google+Apple → JWT HS256, stateful Redis refresh tokens, Stripe subscription accounts, and the GDPR `DELETE /account` lifecycle (in-place anonymisation). No password in V1.
ARCH_BATCH_CONSENSUS | LIVRÉ V0 | batch/ratis_batch_consensus/ARCH_BATCH_CONSENSUS.md:17 | batch consensus pricing trust-score ocr-pattern decay frozen price_consensus | Batch CLI quotidien qui recalcule le `trust_score` des lignes `price_consensus`, détecte Pattern A (OCR aberrant) + Pattern B (nouveaux prix), applique decay temporel et dégèle les consensus `frozen_until` échus.
ARCH_BATCH_MYSTERY_ANNOUNCE | LIVRÉ V0 | batch/ratis_batch_mystery_announce/ARCH_BATCH_MYSTERY_ANNOUNCE.md:17 | batch gamification mystery challenge clues reveal mystery_challenge daily-cycle notifier | Batch CLI quotidien (0h UTC) qui orchestre le cycle des challenges « produit mystère » : révèle progressivement les indices, annonce les trouvailles, active le prochain challenge scheduled, et fait transitionner `active → frozen → revealed`.
ARCH_BATCH_OFF_SYNC | LIVRÉ V0 | batch/ratis_batch_off_sync/ARCH_BATCH_OFF_SYNC.md:17 | batch off open-food-facts open-beauty-facts product-catalog ean sync delta dump multi-source pricing | Batch CLI multi-source (OFF/OBP/OPF/OPFF) qui synchronise le catalogue produits depuis les projets Open\*Facts vers `products`, soit en delta paginé (incrémental), soit en dump JSONL complet (bootstrap initial). Code partagé 100 % entre sources via `--source`.
ARCH_BATCH_ORIGINS_BACKFILL | LIVRÉ V0 | batch/ratis_batch_origins_backfill/ARCH_BATCH_ORIGINS_BACKFILL.md:17 | batch backfill off origins_tags missions french phase-c2 one-shot products idempotent resumable | Batch CLI **one-shot** Phase C-2 sprint missions : parcourt `products WHERE origins_tags IS NULL`, fetch via OFF API `GET /api/v2/product/{ean}?fields=origins_tags`, UPDATE la row. Déverrouille les missions `product_identification + attribute:french` à ≥80 % couverture.
ARCH_BATCH_OSM_SYNC | LIVRÉ V0 | batch/ratis_batch_osm_sync/ARCH_BATCH_OSM_SYNC.md:17 | batch osm stores geo openstreetmap pbf overpass osmium retailers cities proximity ratis_core-geo | Batch CLI quotidien qui sync les magasins OpenStreetMap (PBF streaming via osmium + Overpass incrémental) dans `stores`, `cities`, `retailers`, `retailer_aliases`. **Note 2026-05-15** : la proximité magasins ne lit plus ici mais passe par `ARCH_geo.md` (PostGIS).
ARCH_BATCH_PURGE | LIVRÉ V0 | batch/ratis_batch_purge/ARCH_BATCH_PURGE.md:17 | batch rgpd cleanup aggregation purge volatile sessions scans unknown r2 retention refresh_tokens notifications | Batch CLI quotidien qui agrège les données comportementales pérennes (sessions, scans unknown) PUIS purge les tables volatiles (refresh_tokens, notifications, routes, sessions >90j, scans store_status unknown >7j) et les objets R2 expirés (tickets 48h, labels 72h). Toujours **agréger d'abord, purger ensuite**.
ARCH_BATCH_PUSH_RECEIPTS | LIVRÉ V0 | batch/ratis_batch_push_receipts/ARCH_BATCH_PUSH_RECEIPTS.md:17 | batch notifications push expo receipts polling DeviceNotRegistered cleanup infra user_push_tokens | Batch CLI qui polle les receipts Expo (`push_receipt_tickets`) après envoi push : confirme `DeliveredOK`, classifie `DeliveredError`, invalide les `user_push_tokens` retournés `DeviceNotRegistered`. Cleanup auto des tokens expirés.
ARCH_BATCH_RECONCILIATION | LIVRÉ V0 | batch/ratis_batch_reconciliation/ARCH_BATCH_RECONCILIATION.md:17 | batch reconciliation cab cashback integrity financial balance audit user_cab_balance user_cashback_balance | Batch CLI **financier strict** : vérifie l'intégrité des balances CAB + cashback (sum transactions == balance), détecte les divergences, alerte. Reste 100 % financier (jamais de métier) — séparation argent/data avec `ratis_batch_data_reconciliation`.
ARCH_BATCH_REFERRAL_PAYOUT | LIVRÉ V0 | batch/ratis_batch_referral_payout/ARCH_BATCH_REFERRAL_PAYOUT.md:17 | batch referral gift-cards cashback anti-churn payout 30-days eligible_at referral_uses idempotent | Batch CLI qui paye les gift-cards de parrainage (`referral_uses` éligibles → `gift_card_orders`) une fois le filleul passé la fenêtre anti-churn 30j (`eligible_at`). Idempotent par `UNIQUE(source_type,source_ref_id)`.
ARCH_BATCH_SAVINGS | LIVRÉ V0 | batch/ratis_batch_savings/ARCH_BATCH_SAVINGS.md:17 | batch savings stats snapshot user_savings_snapshot dashboard cab profil daily | Batch CLI quotidien qui calcule le cumul d'économies par user (delta prix scan vs consensus) et stocke un snapshot dans `user_savings_snapshot`. Alimente les stats profil + dashboard mobile.
ARCH_BATCH_SIRENE_SYNC | LIVRÉ V1.0 | batch/ratis_batch_sirene_sync/ARCH_BATCH_SIRENE_SYNC.md:17 | batch sirene stores fr insee geocoding geoplateforme parquet ape whitelist trust-hierarchy | Batch mensuel qui ingère la base SIRENE INSEE (Parquet bulk via data.gouv.fr) dans `stores` pour les établissements alimentaires de France, via geocoding bulk Géoplateforme. Source primaire FR, `ratis_batch_osm_sync` reste source internationale + fallback FR.
ARCH_BATCH_TEMPLATE | LIVRÉ V0 | docs/arch/ARCH_BATCH_TEMPLATE.md:26 | template batch arch boilerplate skeleton convention scaffold meta | Template ARCH for any new Ratis CLI batch: standardised structure (Index, Responsibility, Frequency, Tables, Dependencies, Decisions, Flow, Parameters, Monitoring, FAQ, Glossary) to copy into `batch/ratis_batch_<name>/ARCH_BATCH_<NAME>.md`.
ARCH_BATCH_VRAC_SEED | LIVRÉ V0 | batch/ratis_batch_vrac_seed/ARCH_BATCH_VRAC_SEED.md:17 | batch seed vrac bulk-produce ocr-matching one-shot products internal ean kg fruits-legumes | Script CLI **one-shot** qui ensemence `products` avec ~65 entrées canoniques de vrac FR (fruits, légumes, vracs secs) — EAN interne `2999000000NNN`, `source='internal'`, `unit='kg'`. Cible le matching ticket vrac sans code-barres.
ARCH_CORE | LIVRÉ V0 | ratis_core/ARCH_CORE.md:26 | shared lib auth database config models rewards_client notifier_client core sqlalchemy psycopg pydantic jwt fastapi r18-never-duplicate | Shared Python lib imported by all Ratis services: `auth` (get_current_user), `database` (make_engine, get_db), `deps` (verify_internal_key), `jwt`, `knowledge`, `notifier_client`, `rewards_client`, `schemas`, `settings`, `startup` (require_env), `uploads`, `utils`, `geo`. Never duplicate (R18).
ARCH_EXAMPLE | LIVRÉ V0 | docs/arch/ARCH_EXAMPLE.md:27 | template arch boilerplate skeleton convention scaffold meta service-global example | Global service ARCH template to copy into `webservices/ratis_<svc>/ARCH_<SVC>.md` when creating a new FastAPI service. Includes normalized YAML frontmatter + standardized sections (Index, Responsibility, Endpoints, Tables, Decisions, Flow, Sub-ARCHs, Glossary).
ARCH_LIST_OPTIMISER | LIVRÉ V0 | webservices/ratis_list_optimiser/ARCH_LIST_OPTIMISER.md:27 | list optimiser route osrm shopping-lists shopping_list_items optimized_routes celery redis ttl rgpd | FastAPI service (port 8002) that manages user shopping lists and computes optimised multi-store itineraries via OSRM. Routes cached for 24 h (TTL), `steps` JSONB without a "home" point (PII).
ARCH_NOTIFIER | LIVRÉ V0 | webservices/ratis_notifier/ARCH_NOTIFIER.md:27 | notifier notifications push expo internal fire-and-forget user_push_tokens notification_logs receipts infra | Internal FastAPI service (port 8005) that sends push notifications via Expo, stores device tokens and receipts for audit. Called fire-and-forget by other services using `INTERNAL_API_KEY`.
ARCH_PRODUCT_ANALYSER | LIVRÉ V0 | webservices/ratis_product_analyser/ARCH_PRODUCT_ANALYSER.md:27 | product-analyser scan receipt label barcode ocr paddleocr celery price consensus r2 ean rgpd llm anthropic mistral | FastAPI service (port 8003) that ingests receipt/label/barcode scans from Ratis users, runs an async OCR pipeline (Celery + PaddleOCR), identifies products and stores, maintains multi-user `price_consensus`, and exposes product pages + local prices via EAN.
ARCH_REWARDS | LIVRÉ V0 | webservices/ratis_rewards/ARCH_REWARDS.md:27 | rewards cab cashback gift-cards battlepass missions xp gamification streak mystery leaderboard runa affilae awin cj admin referral | FastAPI service (port 8004) hosting the entire Ratis economy: CAB balance (cabecoin), cashback (Affilae/Awin/CJ), gift cards (Runa post-KYB V1), gamification (battlepass, missions, XP, streaks), mystery challenges, referral, leaderboard.
ARCH_admin_settings | LIVRÉ V0 | docs/arch/ARCH_admin_settings.md:12 | admin settings app_settings runtime-config audit-log shipped rewards-cab-params editables frozen htmx blocs-a-e hardening | Admin Settings UI + runtime config management: editing `app_settings` (section/data JSONB) without curl/Postman, audit log who/when/old/new, segmentation of editables vs frozen (economic parameters DB-editable vs algo parameters PR-only). Shipped Blocs A→E + security hardening.
ARCH_agent_mcp | LIVRÉ V0 | docs/arch/ARCH_agent_mcp.md:32 | mcp agent secrets automation ops security claude-code keychain stdio json-rpc shipped agent-mcp db_tools docs_tools sentry eas github notion stripe r2 vector embeddings bge-m3 sqlite-vec hybrid-search semantic-search agentic-docs | Keychain-backed MCP server `ratis-agent-mcp` that exposes typed tools (Sentry/EAS/GitHub/Notion/Stripe/R2/DB/Docs) to Claude Code without ever putting tokens in the model context. stdio JSON-RPC + macOS Keychain architecture. Modules 1-9 delivered (M8 = db_tools read-only Postgres; M9 = docs_tools hybrid vector+keyword search over the docs, phase D).
ARCH_anti_fraud | LIVRÉ V0 | docs/arch/ARCH_anti_fraud.md:17 | anti-fraud nrc trust-score shadow-ban v1 batch-nightly weight-override grace-period silent-skip users-trust_score | Anti-fraud system V1: `users.trust_score` computed nightly (batch), grace period, thresholds → shadow ban (NRC hook `weight_override` + silent CAB skip). No user-facing screen, invisible action.
ARCH_barcode | LIVRÉ V0 | webservices/ratis_product_analyser/ARCH_barcode.md:11 | barcode ean scan pyzbar product-analyser off lookup | Barcode (EAN) scan pipeline: pyzbar decoding, `products` lookup, OFF fallback if missing, returns product sheet + favourites.
ARCH_battlepass | LIVRÉ V0 | webservices/ratis_rewards/ARCH_battlepass.md:11 | battlepass rewards seasons xp gamification milestones claims progress | Seasonal Battle Pass: `battlepass_seasons` + `battlepass_milestones` + `user_battlepass_progress` + `user_battlepass_claims`. XP + CAB + gift-card rewards per milestone. Rolling season.
ARCH_cab | LIVRÉ V0 | webservices/ratis_rewards/ARCH_cab.md:11 | cab cabecoin rewards balance currency user_cab_balance cabecoin_transactions credit debit atomic rewards_client never-sell | Cabecoin (CAB) — Ratis internal currency. Materialized balance `user_cab_balance`, source-of-truth via `cabecoin_transactions` (`direction credit/debit`, `reference_type` CHECK). Atomic update mandatory, never-sell-CAB red-line.
ARCH_consensus | LIVRÉ V0 | webservices/ratis_product_analyser/ARCH_consensus.md:11 | consensus price trust-score product-analyser price_consensus price_consensus_history frozen-until snapshot V1 | Multi-user price consensus on `(store_id, product_ean)`: `UNIQUE` per pair, `trust_score`, `price_consensus_history` on every change, `frozen_until` when score ≥95%. Parameters in `ratis_settings.json`.
ARCH_example_sub | LIVRÉ V0 | docs/arch/ARCH_example_sub.md:18 | template arch boilerplate sub-arch convention scaffold meta example feature | Template for a sub-ARCH (feature within an existing service or cross-cutting ARCH) to be copied as `ARCH_<feature_snake>.md`. Includes Index/Checklist/Context/Decisions/Tests sections. Counterpart to `ARCH_EXAMPLE.md` on the service-global side.
ARCH_gamification | LIVRÉ V0 | webservices/ratis_rewards/ARCH_gamification.md:11 | gamification xp streak feed-jack buffer burst achievements tirelires prestige photo-hash mission-freeze | Cross-service gamification layer (rewards + product_analyser): photo hash, XP, mission freeze, Feed Jack streak, achievements, tirelires, prestige. Buffer + Burst pending.
ARCH_geo | LIVRÉ V0 | docs/arch/ARCH_geo.md:25 | geo postgis proximity infra ratis_core stores spatial-index geoalchemy2 haversine-replace osm-deprecated | Shared Ratis geospatial layer: PostGIS spatial index on `stores` + stateless Python module `ratis_core.geo` that replaces proximity searches by full-table-scan + manual haversine. Migrated consumers see dedicated section.
ARCH_missions | LIVRÉ V0 | webservices/ratis_rewards/ARCH_missions.md:11 | missions rewards daily weekly quests buffer burst user_missions xp cab catalog completion | Daily/weekly missions: `missions` (catalogue) + `user_missions` (progress), CAB + XP rewards, battlepass and gamification integration. Buffer/Burst on XP.
ARCH_ocr_store_detection | LIVRÉ V0 | docs/arch/ARCH_ocr_store_detection.md:10 | ocr store-detection receipt fingerprint product-analyser store_detector ocr_knowledge city store_fingerprint store_candidate ratis_core-geo | Store detection from receipt OCR: `store_detector.py` (fingerprint + match), `OcrKnowledge`, `City`, `StoreFingerprint`, `StoreCandidate`. Internal pipeline (no public endpoint). **Note 2026-05-15**: store proximity search now via PostGIS `ratis_core.geo`.
ARCH_referral | LIVRÉ V0 | docs/arch/ARCH_referral.md:10 | referral parrainage rewards gift-cards anti-churn eligible_at filleul parrain code souscription payout batch v1 | Referral programme V1: referrer shares their code, referred user signs up + receives a signup bonus, referrer payout via gift card upon subscription to a paid plan after 30-day anti-churn delay (`eligible_at`). Backend + frontend + batch payout shipped.
ARCH_scan | LIVRÉ V0 | ratis_client/ARCH_scan.md:11 | scan camera receipt label upload-queue client takePictureAsync fullscreen overlays glass theme-v2 store-detection-backend | Fullscreen camera scan screen with glass overlays: history top-left, photo counter + Send top-right, mode switch (receipt/label) + gold capture button bottom. Real upload (no more mock): receipt → POST /receipt, label batch → POST /label/batch (with geo).
ARCH_seed_test_data | LIVRÉ V0 | docs/arch/ARCH_seed_test_data.md:1 | seed test-data ratis_seed personas stores products receipts consensus subscriptions gift-cards withdrawals shipped-v0 waves dev-db prng make-seed-rebuild | Deterministic seed test data for the local dev DB: 6 personas, 14 stores, 25-26 products, 500 receipts / 3 567 scans, 274 price_consensus, 5 subscriptions, 8 gift_card_orders, 5 withdrawals, 10 product_knowledge. Waves 1-5 shipped 2026-05-11, Step 4 gamification BLOCKED.
ARCH_store_resolution | LIVRÉ V0 | webservices/ratis_product_analyser/ARCH_store_resolution.md:11 | store-resolution osm overpass matcher product-analyser retailer_aliases store_candidates cold-start identify-store deprecated-partiel ratis_core-geo | Store resolution from a scan: local `retailer_aliases` + `stores` matcher (DA-35, replaces real-time Overpass). The `identify-store` endpoint (free-text input) is deprecated 2026-04-29 — replaced by `confirm-store` (PR-B) for anti-abuse reasons. The internal pipeline remains in place.
DA-11 | LIVRÉ V1.1 | docs/arch/ARCH_n8n_pipelines.md:304 | db-write-pipeline n8n agent-mcp db_propose_write hub overview V1.1 hardening HSP audit | n8n `db-write-pipeline` workflow receives HMAC `db_propose_write` proposals (agent-mcp) → sandbox → dry-run → LLM review → human gate → prod execution. SP1+SP3+SP4+SP5+SP6 delivered V1 (backbone + 2-pass LLM + mirror UI gate `db_write_approvals`). V1.1 hardens via HSP-1 to HSP-5 (atom catalogue + DB floor + unforgeable gate + agent confinement + swap prep). Post-delivery cleanup: DA-14/15/16.
DA-12 | LIVRÉ V1.0 | docs/arch/ARCH_n8n_pipelines.md:524 | batch-sentinel n8n push-monitoring composite-action HMAC anti-replay Notion External-ID dedupe Discord digest GH-Actions continue-on-error Phase-1 sentry-ingest-pattern | Phase 1 passive monitoring. The 10 GH Actions batch workflows (off_sync, achievements, consensus, purge, savings, referral_payout, annual_reset, osm_bulk_sync, vrac_seed, origins_backfill) **push** their final outcome to n8n via shared composite action `report-batch-outcome` (HMAC-SHA256 + anti-replay timestamp ±300s). n8n applies the `sentry-ingest` pattern (Notion lookup by External ID `batch:<workflow_name>` → new/reopen/increment branch → red Discord alert + quarantine fallback). Cron 09h05 Europe/Paris posts a 24h Discord digest aggregated from the workflow's own static data.
DA-13 | LIVRÉ V1.0 | docs/arch/ARCH_n8n_pipelines.md:610 | inventory convention doc-rework arch-inventory grep-friendly batch-A | End-to-end demo of the new `## <ID> — <title> · <refs> · <STATUS>` convention indexed in `ARCH_INVENTORY.md` (Batch A inventory-rework). All long-lived ARCHs will migrate progressively (Batch B).
DA-14 | LIVRÉ V1.1 | docs/arch/ARCH_n8n_pipelines.md:552 | audit cleanup M1 M5 M7 L1 L3 L4 ratis_sandbox argOrder sqlQuote llm_unavailable EXECUTE_ENABLED redact_args sandbox-id-validation defense-depth | Cleanup post HSP-1→HSP-4 of 6 findings from audit `AUDIT_2026-05-19_db_write_pipeline.md`. 3 immediate fixes (M1 `Money-table scan` was querying non-existent `ratis_sandbox` → `ratis_prod`; M5 non-deterministic `args` order → `argOrder = manifest.args.map(a => a.name)`; L3 `_llm_unavailable` silent fail-open → explicit catch). 3 new corrections (M7 regex validation of `_sandbox_id`/container name before shell interpolation; L1 hardcoded `EXECUTE_ENABLED` → `$env.DB_PIPELINE_EXECUTE_ENABLED === 'true'` fail-safe; L4 `redact_args` truncates `client_message`+`investigation` to 100 chars). 3 M/L DROPPED (M2/M3/M4 auto-resolved by HSP1-4).
DA-15 | LIVRÉ V1.1 | docs/arch/ARCH_n8n_pipelines.md:575 | audit L2 anthropic-api cost-unbounded rate-limit throttle defense-in-depth deque sliding-window 10-req-min 20-req-min PROD_CHECKLIST monthly-cap | Closes finding L2 (unbounded Anthropic API cost) via 3 defense-in-depth layers against unbounded API consumption (2 LLM calls per proposal on the n8n side). Layer 1 agent-mcp in-memory throttle `db_propose_write` 10 req/min sliding window (deque separate from `db_query` 60/min). Layer 2 n8n workflow node `Rate limit webhook` (sliding window 20/min via `$workflow.staticData`) + IF + Respond 429, inserted between `IF HMAC OK [true]` and `Validate identity`. Layer 3 PROD_CHECKLIST entry monthly spend cap ~50€ on n8n Anthropic API key.
DA-16 | LIVRÉ V1.1 | docs/arch/ARCH_n8n_pipelines.md:590 | audit M6 sandbox sec-posture chmod-700 docker-isolated-network port-mapping retention 24h SNAPSHOT_MAX_AGE_MINUTES PRIVACY-update PROD_CHECKLIST quick-wins | Quick wins M6 sandbox (encryption at-rest + anonymisation on restore remain V2 pre-third-party-user opening). QW1 `chmod 700` on `~/.local/share/ratis/db-sandbox/` + `snapshots/` + `chmod 600` on `.sql.gz`. QW2 dedicated isolated Docker network per sandbox (`ratis_sandbox_isolated_<id>`) created by `sandbox-up.sh`, deleted by `sandbox-down.sh`, no `-p` port mapping (accessible only via `docker exec`). QW3 snapshot retention 7d → 24h via `find -mmin +1440`, tunable `SNAPSHOT_MAX_AGE_MINUTES`. QW4 dedicated section in `PRIVACY.md` (Mac mini as processing location) + M6 V2 entry in `PROD_CHECKLIST.md`.
DS-1 | LIVRÉ V0 | docs/arch/ARCH_doc_system.md:35 | arborescence repo root docs-tree categorization ops audits | TL;DR Agent doc = `docs/<cat>/` (arch · product · known · decisions · ops · seed). Stay-at-root exceptions are legitimate: agent contracts (CLAUDE/ORCHESTRATOR/SA_*), auto-gen indexes (ENDPOINTS/ARCH_INVENTORY), PROD_CHECKLIST, and the local-only journal (SESSION_LOG/DECISIONS_PENDING).
DS-2 | LIVRÉ V0 | docs/arch/ARCH_doc_system.md:56 | r41 format heading id status tldr tags subs convention pipe-separated | TL;DR Each major section: `## <ID> — <title> · <refs> · <STATUS>` + quote-block `> TL;DR` (1-2 sentences) + `> @tags: space-separated words` + `> @subs: auto`. IDs `[A-Z]+-N` (DA, KP, HSP, M, DS…). Statuses: `LIVRÉ` ∣ `EN-COURS` ∣ `PLANIFIÉ` ∣ `DEPRECATED` (+ free suffix like `LIVRÉ V1.1`).
DS-3 | LIVRÉ V0 | docs/arch/ARCH_doc_system.md:78 | lifecycle spec plan brainstorm writing-plans distillation transitory git-history | TL;DR Spec/plan = dense draft produced by a brainstorm or writing-plans skill. **Transitory**: distilled into the target ARCH (10-30 dense lines), then removed from the tree post-merge. Git history preserves the draft.
DS-4 | LIVRÉ V0 | docs/arch/ARCH_doc_system.md:96 | consommation docs-mcp docs-search docs-get docs-find workflow agent-reading r29 fallback | TL;DR Default: `docs_search(query, top_k=5)` → `docs_get(id)` for body → optional `docs_find(status=,tags=,file_glob=)` for typed lists. Fallback `Grep ARCH_INVENTORY.md` + `Read offset=<line>` if docs-mcp is down. Never full-read R29.
DS-5 | LIVRÉ V0 | docs/arch/ARCH_doc_system.md:116 | ecriture extend new-arch r41-strict template frontmatter regen inventory check-convention | TL;DR Before writing a new ARCH: `codebase-recon` checks that there is no adjacent ARCH to extend. If new: strict R41 format (YAML frontmatter + heading + TL;DR + @tags + @subs). After writing: `generate-arch-inventory.py` + `check-arch-convention.sh` must stay green.
DS-6 | LIVRÉ V0 | docs/arch/ARCH_doc_system.md:141 | deprecation removal breadcrumb successor git-history index-preservation | TL;DR Keep the index entry as `STATUS: DEPRECATED` + reason + link to successor. Delete the file = `git rm` + breadcrumb in the successor (`> Successor of [[ARCH_X]] — deleted <date>, see git show <SHA>:<path>`).
DS-7 | LIVRÉ V0 | docs/arch/ARCH_doc_system.md:162 | notion-export skill human-layer on-demand decision-maker dry-run idempotent external-id | TL;DR Skill `notion-export` (#562) generates a decision-maker-readable Notion version from a subset of the documentation (selected via docs-mcp). Idempotent via External ID `ratis-export:<id>`. Dry-run mode available. **Not for daily use — invoked on demand** (board review, operator sharing, non-tech alignment).
HO-1 | LIVRÉ V0 | docs/arch/ARCH_hermes_ops.md:54 | telegram gateway pairing soul persona memory holographic voice stt tts whisper | TL;DR: bidirectional Telegram bot `@RatisAppBot`, concise FR persona `SOUL.md`, 6h memory + holographic, voice-in STT (faster-whisper local), optional voice-out TTS (edge FR, `auto_tts` toggleable).
HO-2 | LIVRÉ V0 | docs/arch/ARCH_hermes_ops.md:68 | cron daily-digest github-watch backup codex-reset hermes-cron scripts deterministic | TL;DR: 4 `hermes cron --no-agent` jobs (deterministic, 0 LLM except postmortem): daily-digest 09h, github-watch /15min, auto-codex-reset /h, hermes-state-backup 05h. Scripts in `~/.hermes/scripts/` (versioned under `infra/hermes/hermes-home/scripts/`).
HO-3 | LIVRÉ V0 | docs/arch/ARCH_hermes_ops.md:85 | kanban tracker snapshot restore decisions-pending frontière source-of-truth | TL;DR: Hermès kanban = **tracker for ephemeral todos/ops** (lifecycle todo→done), `dispatch_in_gateway: false` (no autonomous execution). JSON+MD snapshot versioned under `infra/hermes/kanban-snapshot.*` (re-importable, see kanban-restore.sh). Does NOT replace DECISIONS_PENDING/ACTED.
HO-4 | LIVRÉ V0 | docs/arch/ARCH_hermes_ops.md:106 | postmortem reviewer skills candidates explores opus routine claude-ai anti-injection self-learning DA-48 sentinel | TL;DR: 2 Claude.ai routines (Pro/Max sub, 0€ marginal) — postmortem-deep (Opus + 5 parallel Explores, anti-timeout) mines sessions → skill-candidates; reviewer (triple-validation anti-injection) validates → operator promotes. Self-learning loop (DA-48).
HO-5 | LIVRÉ V0 | docs/arch/ARCH_hermes_ops.md:119 | mcp serve claude-code stdio bidirectional messages-send agent-mcp | TL;DR: `hermes mcp serve` registered as an MCP stdio server in Claude Code (`claude mcp add hermes`) → 10 tools (messages_send, conversations, events, …). Claude Code can drive Hermès (push Telegram, read convos). Distinct from our custom agent-mcp (which remains the Keychain-backed typed tools layer).
HO-6 | LIVRÉ V0 | docs/arch/ARCH_hermes_ops.md:131 | infra-as-code deploy backup versioning secrets-template keychain detect-secrets recovery runbook | TL;DR: the entire stack (secrets-stripped config templates, scripts, postmortem skill, routines, compose, glitchtip, glt) versioned under `infra/hermes/` + idempotent `deploy.sh`. Before: everything lived locally unsaved. Secrets → `${VAR}` read from Keychain, CI detect-secrets gate.
HO-7 | LIVRÉ V0 | docs/arch/ARCH_hermes_ops.md:144 | tech-debt R33 adapter auto-codex-reset hmac-proxy fallback-provider workaround honest | TL;DR: 2 pragmatic adapters honestly tracked vs R33 — auto-codex-reset (band-aid on the Codex exhausted flag; real fix = fallback provider, deferred for lack of a 2nd key) and the GlitchTip→Hermès HMAC proxy (sidecar because GlitchTip does not sign its webhooks).
HSP-1 | LIVRÉ V1.1 | docs/arch/ARCH_n8n_pipelines.md:335 | db-pipeline catalogue manifeste TOML pglast verifier atome procedure-stockee support_credit_cab support_debit_cab support_link_scan_to_user single-row money_tier direction facing audit-V1 hardening C1 C3 H6 M5 | TOML sidecar manifest per stored procedure (purpose, facing, direction, money_tier, args, affects). `pglast` verifier with 8 checks at merge time + defense in depth in `apply_procedure`. 3 initial atoms: `support_credit_cab`, `support_debit_cab`, `support_link_scan_to_user`. Guards: single-row, fixed direction, explicit money_tier, OUT integer rows_affected. Kills findings C1 (free-form runtime SQL) and C3 (auto-written invariants).
HSP-2 | LIVRÉ V1.1 | docs/arch/ARCH_n8n_pipelines.md:367 | db-pipeline plancher BDD change-log triggers caps dormants kill-switch app_settings cabecoin_transactions 20k 50k 5k submission_id append-only HSP2 audit V1.1 hardening | Append-only `db_change_log` table populated by generic trigger on 6 sensitive tables (`to_jsonb(OLD)`/`to_jsonb(NEW)` tagged `submission_id`). Dormant temporal caps on `cabecoin_transactions` (double kill-switch session+settings, 20k/50k global + 5k per-user CAB/24h thresholds). Layer ⑦ of the V1.1 model — the DB structurally bounds the worst case, independent of pipeline/LLM/human/agent.
HSP-3 | LIVRÉ V1.1 | docs/arch/ARCH_n8n_pipelines.md:402 | db-pipeline gate-humain challenge HMAC argon2id summary-fr anomaly-flags graduation trust-level HSP3 M1 M2 M3 M4 M5 C4 H3 audit V1.1 hardening admin-ui | 5 structural mechanisms against rubber-stamping and agent bypass. M1 challenge to type systematically (case+space insensitive, 3 attempts → 60s lockout, anti-paste `user-select:none`). M2 distinct HMAC secret `HUMAN_APPROVAL_SECRET` (argon2id, never in plaintext in DB/env/log). M3 deterministic French summary (n8n Code node, not LLM). M4 5 structural anomaly flags frozen at Register approval. M5 graduation `trust_level ∈ {manual, caps_only, frozen}`. Kills findings C4 + H3 structurally, mitigates H4/H2/M1.
HSP-4 | LIVRÉ V1.1 | docs/arch/ARCH_n8n_pipelines.md:440 | db-pipeline confinement-agent agent_read rôle-PG REVOKE identity-check schema-validation no-SQL-runtime checksum freeze trust-level anti-exfil HSP4 M1 M2 M3 M4 M5 M6 M7 C1 C2 H1 H4 H5 H6 M3 audit V1.1 hardening EXECUTE_ENABLED | 7 mechanisms: REVOKE-restricted PG `agent_read` role (M1), `agent_id`+`proposed_at` HMAC identity re-verification (M2), runtime dynamic Pydantic schema validation (M3), no-SQL runtime `mode:new` removed (M4), per-call checksum via `db_change_log` post-`CALL` + `trust_level` freeze (M5), anti-exfil cap 1000 rows + 60/min throttle + payload ≤32 kB (M6), idempotent UP+DOWN migration (M7). `EXECUTE_ENABLED=true` flipped. Effective swap remains HSP-5.
HSP-5 | LIVRÉ prep | docs/arch/ARCH_n8n_pipelines.md:479 | db-pipeline swap bootstrap-vs-verrouille HSP5 prep inventaire-acces SSH runbook MCP rôle-scopé Tailscale clé-prod open-questions runbook transition-graduelle audit V1.1 | Preparatory phase **with no irreversible act**. Inventory of the 11 surfaces where the agent borrows operator identity (SSH `ratis_hetzner_v3`, `gh` CLI, MCP Keychain, n8n admin UI, Tailscale Funnel, EAS, shell env `EXPO_TOKEN`, Hetzner `.env.prod`, `infra/itops/.env`, Anthropic API keys, GH Actions runners). Design of the scoped MCP role post-swap (`db_query` recommended transport: Tailscale + psycopg). 10-step swap runbook (backup `authorized_keys` → pre-flight checks → SSH agent key removal → verify n8n workflows → migrate `EXPO_TOKEN` Keychain → change n8n admin password → final e2e run → announcement). 8 open questions to settle before actual HSP-5.
IM-1 | LIVRÉ V0 | docs/arch/ARCH_incident_management.md:128 | docker-compose 5-services postgres-16-alpine valkey-7 granian celery loopback-only isolation-resources | TL;DR: `~/glitchtip/docker-compose.yml` orchestrates 5 services (web + worker + postgres + valkey + one-shot migrate). Port 8000 on loopback only (Tailscale Funnel possible later for external ingestion). Dedicated PostgreSQL and Valkey (not shared with ratis-postgres or ratis-redis for isolation). Total RAM ~600 MB. Resource limits: web 1 GB / worker 512 MB.
IM-2 | LIVRÉ V0 | docs/arch/ARCH_incident_management.md:157 | 3-projects ratis-mobile ratis-backend n8n-workflows dsn keychain-ops convention | TL;DR: 3 GlitchTip projects created via API at setup, 1 public DSN per project, stored in Keychain. Convention: `ratis-<scope>` for the project name, `ops-glitchtip-dsn-<scope>` for the Keychain DSN account. SDK → project routing is handled by the DSN injected into each service's config.
IM-3 | LIVRÉ V0 | docs/arch/ARCH_incident_management.md:181 | ingest sdk-direct sentry-compat n8n batch-sentinel sunset-sentry-ingest pr-merged-closer | TL;DR: 2 ingestion paths to GlitchTip. (1) **Direct SDK via DSN** for Expo + Python services (Sentry-compatible, 0 custom code). (2) **n8n `batch-sentinel.json`** for GH Actions batch outcomes (which do not run a Sentry SDK). Historical workflow `sentry-ingest.json` **deleted** (SDKs point directly, no more relay). Workflow `github-pr-merged-closer.json` adapted to PATCH GlitchTip issue instead of Notion page.
IM-4 | LIVRÉ V0 | docs/arch/ARCH_incident_management.md:239 | wrapper-bash glt cli token-keychain api-glitchtip 9-commands no-secret-exposure | TL;DR: `~/glitchtip/bin/glt` is a lightweight bash wrapper (~250 lines) that encapsulates GlitchTip API calls. The admin token is read from the Keychain on each call, never exposed in plaintext in the code or in outputs (except via an explicit flag). 9 subcommands covering common operations (list, add, remove, resolve, status, show-dsn, orgs, teams, help).
IM-5 | LIVRÉ V0 | docs/arch/ARCH_incident_management.md:274 | sunset migration zero-data-loss 2026-05-31 1-pr propre clean-break | TL;DR: Notion sunset in a single pass (~1 PR), no data migration (the 2 historical incidents visible in Ratis INCIDENTS Notion were throwaway smoke tests — Guillaume's decision 2026-05-31). Removal of the `notion_tools.py` module (agent-mcp), removal of the `sentry-ingest.json` workflow (n8n), adaptation of `batch-sentinel.json` + `github-pr-merged-closer.json` to point to GlitchTip. Notion token deleted from Keychain. Notion integration revoked from the Notion UI by Guillaume.
AMI-1 | PLANIFIÉ | docs/arch/ARCH_agent_mcp_isolation.md:120 | hermes-runtime user-os dscl hermes-bridge group no-sudo no-login uid keychain-vide | TL;DR : create a standard OS user `hermes-runtime` (non-admin, not in sudoers) via `dscl`, member of the `hermes-bridge` group. UID outside the admin range (≥ 510). No interactive login shell (`/usr/bin/false` or `/sbin/nologin`). Its login Keychain is empty; no Ratis secret ever lives there.
AMI-2 | PLANIFIÉ | docs/arch/ARCH_agent_mcp_isolation.md:169 | daemon launchd transport-swap stdio-socket lifecycle signal-handlers reload-config briques-v0-réutilisées | TL;DR : add a daemon mode to `agent-mcp` (transport swap stdio → Unix socket) supervised by launchd. Core V0 bricks (Dispatcher, AuthGate, AuditLog with flock, Keychain wrapper with 60s cache, decorator registry, regex redaction) **remain unchanged** — the redesign is limited to transport and lifecycle.
AMI-3 | PLANIFIÉ | docs/arch/ARCH_agent_mcp_isolation.md:206 | socket-unix peer-cred SO_PEERCRED getpeereid uid-mapping permissions-0660 group-hermes-bridge kernel-trust | TL;DR : Unix socket `/tmp/ratis-agent-mcp.sock` (or `~/Library/Application Support/ratis-agent-mcp/agent-mcp.sock` cleaner), mode 0660 owner=guillaume group=hermes-bridge. Peer-cred auth via `getpeereid()` syscall — the UID is extracted from the socket by the kernel, impossible to spoof without OS escalation.
AMI-4 | PLANIFIÉ | docs/arch/ARCH_agent_mcp_isolation.md:249 | hermes-bridge mcp-transport stdio-over-socket socat wrapper-cmd upstream-pr investiguer | TL;DR : Hermes speaks MCP natively over stdio. To reach a Unix socket, two options: (a) Hermes config points to a wrapper command `socat UNIX-CONNECT:/tmp/ratis-agent-mcp.sock STDIO` that bridges stdio↔socket per connection, or (b) add a native socket transport on the Hermes side (upstream PR NousResearch). Option (a) in V1, option (b) if the Hermes docs already support it or if we contribute.
AMI-5 | PLANIFIÉ | docs/arch/ARCH_agent_mcp_isolation.md:277 | migration progressive flip-switch poc-no-friction backward-compat phase-1a phase-1b uc0 dual-transport | TL;DR : no big-bang switch. Prepare the rail (AMI-1 + AMI-2 + AMI-3) in parallel with the Hermes POC (Phase 1a-2 + 1b under `guillaume` direct). When UC0 token-fetcher is validated on 5 providers, switch Hermes to `hermes-runtime` + socket. The V0 stdio mode remains operational for Claude Code.
AMI-6 | PLANIFIÉ | docs/arch/ARCH_agent_mcp_isolation.md:306 | tests-adversariaux red-team integration ci-tests peer-cred-validation keychain-isolation injection-prompt fuzz | TL;DR : a test suite that *attacks* the isolation system to verify it holds. Run automatically in CI + at flip switch Phase 2 → Phase 3, and quarterly thereafter.
ARCH_BATCH_DATA_RECONCILIATION | PLANIFIÉ | batch/ratis_batch_data_reconciliation/ARCH_BATCH_DATA_RECONCILIATION.md:17 | batch reconciliation data nrc esl retro-cab mdd-vote price-disambiguate planned | Batch nightly planifié qui réconcilie les données métier (NRC EAN partiel, items en disambiguation, receipts ambigus multi-store, scans nouvellement résolus) post-sync nuit (consensus, OFF, SIREN) pour déclencher CAB rétroactif + notifs gratitude. Séparé volontairement du batch financier `ratis_batch_reconciliation`.
ARCH_BATCH_LEADERBOARD | PLANIFIÉ | batch/ratis_batch_leaderboard/ARCH_BATCH_LEADERBOARD.md:17 | batch leaderboard gamification ranking cab planned vue-materialisee snapshot | Batch CLI **planifié** (pas encore codé, dossier batch vide) qui calculera le classement CAB des users dans `leaderboard_current` + `leaderboard_snapshots`. Vue matérialisée + rotation périodique.
ARCH_cross_retailer_consensus | PLANIFIÉ | webservices/ratis_product_analyser/ARCH_cross_retailer_consensus.md:13 | consensus name-resolution cross-retailer esl retailer source-of-truth pipeline-v3 product_name_resolutions verified planned-2026-05 | Extension of the NRC (Name Resolution Consensus): promotion of `(label) → product_ean` cross-store (not per-store) for V1 scale, and elevation of the ESL (electronic shelf label) as the product-in-store source-of-truth. Plan blocks A-H.
ARCH_frontend_strict_iso | PLANIFIÉ | ratis_client/ARCH_frontend_strict_iso.md:13 | frontend design-fidelity claude-design ratis_client expo react-native strict-iso handoff design-system pixel-perfect planned 2026-05-04 | "Strict iso" rule from the Claude Design handoff: reproduce visually identically what is in the handoff bundle. No visual simplification, no invented components, no undocumented variants. Implementation dispatched to SA Dev + audit by SA Reviewer.
ARCH_mystery_product | PLANIFIÉ | webservices/ratis_rewards/ARCH_mystery_product.md:11 | mystery-product rewards gamification notification mystery_challenges clues finds challenge cab planned | "Mystery product" challenges: a hidden product to discover through progressive clues over the period. `mystery_challenges` + `mystery_challenge_clues` + `mystery_challenge_finds`. CAB reward for the first finder + participation bonus. Planned.
ARCH_receipt_pipeline | PLANIFIÉ | webservices/ratis_product_analyser/ARCH_receipt_pipeline.md:13 | pipeline receipt ocr parsing matching knowledge contract-test redesign v3 silent-drop audit planned 2026-04-30 | Planned redesign of the receipt pipeline (image → structured DB data) with a single contract, zero silent drops, and full traceability. Built in parallel with the existing pipeline following audit PR #191 (28 silent drops, including 8 on the alpha receipt).
ARCH_store_validation | PLANIFIÉ | webservices/ratis_product_analyser/ARCH_store_validation.md:13 | store-validation user-suggested consensus anti-abuse cashback-retroactive product-analyser pr-b validation_status suggested_by_user_id confirm-store | Workflow `user_suggested → confirmed` for stores suggested via OCR: `stores.validation_status`, `stores.suggested_by_user_id`, flip via consensus batch phase 3. Retroactive cashback for pre-confirmation scans. **Planned** 2026-04-29.
IM-6 | PLANIFIÉ | docs/arch/ARCH_incident_management.md:312 | futur source-maps performance-monitoring scaling-hetzner v1-plus | TL;DR: planned extensions post-V0 based on real volume and needs. **Not urgent**, to be scoped when the pain actually manifests. Expo source maps + perf monitoring + Hetzner scaling remain the 3 main axes.
DECISIONS_ACTED | LEGACY | docs/decisions/DECISIONS_ACTED.md:1 |  | Validated and enacted decisions. Permanent reference — never delete.
KNOWN_PROBLEMS | LEGACY | docs/known/KNOWN_PROBLEMS.md:1 |  | Catalogue des problèmes récurrents et leurs solutions propres.
