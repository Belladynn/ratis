# Visual assets

Static assets referenced by the root `README.md` and the docs set.

## App screenshots

Real, in-app screenshots captured from the Expo / React Native client (`ratis_client/`), one per core flow:

| File | Screen | Illustrates |
|------|--------|-------------|
| `app-dashboard.png` | Home / Dashboard | Gamification surface — battle pass level, season progress, cabecoin (CAB) and cashback balances. |
| `app-product-prices.png` | Product | Real-time per-`(store × product)` price consensus across nearby retailers, sorted cheapest-first. |
| `app-route-optimised.png` | List → Itinerary | Optimised multi-store shopping route with per-stop savings and total distance/time. |

Source masters live under `ratis_client/Ratis_handoff/screenshots/`. UI copy is French (the V0 locale).

## Still to add

<!-- TODO: <10s demo GIF of the scan → price → reward loop, exported as docs/assets/demo.gif and embedded in the README demo slot. -->

- `demo.gif` — a short (<10s, optimised) screen capture of the end-to-end `scan → price → reward` loop, for the README "Demo" section.
